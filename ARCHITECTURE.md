# Architecture

## Overview

Single deployable SvelteKit application (adapter-node) over PostgreSQL. Server-side
rendering with progressive enhancement: every page and form works without JavaScript;
`use:enhance` upgrades the experience when JS is available.

```
Browser ──► SvelteKit routes (load / form actions)
                 │        └── Zod validation (src/lib/schemas — shared client/server)
                 ▼
           Service layer (src/lib/server/services)
                 │        └── business logic + audit log writes, in transactions
                 ▼
           Drizzle ORM (src/lib/server/db) ──► PostgreSQL 16
```

## Layers & conventions

| Layer    | Location                  | Rules                                                                                                                        |
| -------- | ------------------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| Routes   | `src/routes`              | Thin: parse/validate input, call a service, shape the response. No SQL in routes.                                            |
| Schemas  | `src/lib/schemas`         | Zod schemas shared by server actions and client hints. Form-friendly: empty strings become `undefined`, numbers are coerced. |
| Services | `src/lib/server/services` | All business logic. Mutations run in a transaction that also writes the `audit_log` entry.                                   |
| Data     | `src/lib/server/db`       | Drizzle schema + client. Migrations generated by drizzle-kit live in `drizzle/` and are committed.                           |
| UI       | `src/lib/components`      | Reusable Svelte 5 components (forms, modal, toasts).                                                                         |

## Navigation

The sidebar (`src/routes/(app)/+layout.svelte`) is organized by task rather than by table.
Five primary areas lead — Home, Bill Management, Utility Accounts, Plant Management, and
Facility Management — followed by an "Insights & Work" group (Clients, Energy Data,
Analysis, Projects, Tasks, Documents).

Two areas are hubs rather than entities of their own:

- **`/facilities`** fronts the physical hierarchy, linking campuses, complexes, buildings
  and meters with live counts drawn from the existing list services.
- **`/plants`** is a deliberate placeholder — generation assets (central plants, solar,
  storage, CHP, water resources) need a data model distinct from consumption meters, so
  the nav slot exists while the schema is designed separately.

Because `/utilities/bills` and `/utilities/accounts` are siblings, a plain prefix match
would highlight both. A `sectionRoutes` map declares which paths each nav item owns —
Facility Management owns `/campuses`, `/complexes`, `/buildings` and `/utilities/meters` —
so exactly one item is ever active. `tests/e2e/navigation.spec.ts` asserts this per route.

## Physical hierarchy (the built environment)

Assets are modeled under a client the way MUSH (Municipal / University / School /
Hospital) and federal portfolios are actually organized:

```
Client
 └── Campus            (optional grouping — e.g. a university campus or installation)
      └── Complex       (optional "premise": buildings served by one master meter;
      │                  "Campus" in DOE BEDES; maps to maintenance districts)
      └── Building       (a single enclosed structure)
            └── Meter    ── premise is a Building XOR a Complex ──┐
                 └── Submeter (parent_meter_id → meter, same utility type)
```

- **Campus** and **Complex** are both optional. A building can belong to a campus, a
  complex, both, or neither; `buildings.campus_id` / `complex_id` are nullable and
  `ON DELETE SET NULL`, so removing a grouping never deletes the buildings under it.
- **A meter's premise is exactly one of a building or a complex** — enforced by the
  `meters_premise_chk` check constraint (`building_id XOR complex_id`). A complex meter
  is the "master" serving several buildings; a building meter is the common case.
- **Submeters** hang off any meter via the self-referential `parent_meter_id`. The
  meters service (`assertValidMeter`) requires a submeter to share its parent's utility
  type and rejects self-references and cycles; `is_submeter` is derived from the parent
  link. Energy readings and utility bills reference `meter_id`, so a complex master
  meter carries its own readings and bills with no extra tables.
- **Future:** plants and distributed energy & water resources (DER) are a planned
  additional premise/asset type alongside Building and Complex — the nullable-premise
  and master/submeter model already leaves room for them.

## Authentication & authorization

- **better-auth** with email/password; sessions persisted in Postgres (`session` table),
  HTTP-only cookies. The auth HTTP handler is mounted at `/api/auth/*` via
  `svelteKitHandler` in `hooks.server.ts`.
- Public self-registration is disabled (`ALLOW_SIGNUP` gates it; the seed script uses it).
- `hooks.server.ts` resolves the session on every request into `locals.user` /
  `locals.session`, and **server-enforces** that everything under the `(app)` route group
  requires a signed-in user.
- Roles: `admin`, `consultant` (write), `client` (read-only). Write actions call
  `requireRole(locals.user, WRITE_ROLES)` from `src/lib/server/authz.ts` — authorization
  lives on the server, never only in the UI.

## Audit logging

Every create/update/delete on a domain entity writes an `audit_log` row (actor, entity,
entity id, action, shallow field diff) in the same transaction as the mutation
(`src/lib/server/services/audit.ts`). The dashboard surfaces recent activity.

## File storage

Uploaded documents live on the local filesystem under `UPLOAD_DIR` (default
`./uploads`), stored under generated UUID names — user-supplied file names never
touch a filesystem path. Downloads stream through an authenticated endpoint with
`Content-Disposition: attachment` so uploaded content never executes in the
app's origin. The storage calls are isolated in
`src/lib/server/services/documents.ts` for a later swap to object storage.

## Observability

- **pino** structured logging; each request gets a UUID request id (returned as the
  `x-request-id` response header) and a child logger on `locals.log`.
- Request completion lines include method, path, status, duration, and user id.

## Environment & configuration

Configuration comes from environment variables only (`.env` in development — see
`.env.example`). Server modules read `process.env` (not `$env`) so the same code runs
under SvelteKit, Vitest, and tsx scripts; `db` and `auth` initialize lazily so
SvelteKit's build-time analysis can import them without env vars.

## Testing strategy

| Suite                      | Tool       | Target                                                                                                          |
| -------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------- |
| Unit + service integration | Vitest     | Services and schemas against a real Postgres test DB (`puds_test`). Sequential files, truncation between tests. |
| End-to-end                 | Playwright | Production build + seeded DB: auth flows, CRUD journeys, role authorization.                                    |

CI runs lint → typecheck → unit → build → e2e with a Postgres service container.

## Adding a new entity (the pattern)

1. Add the table to `src/lib/server/db/schema.ts`; `npm run db:generate`.
2. Add a Zod schema in `src/lib/schemas/<entity>.ts`.
3. Add a service in `src/lib/server/services/<entity>.ts` (list/get/create/update/delete
   with audit writes).
4. Add routes under `src/routes/(app)/<entity>/` following clients/buildings.
5. Add service tests in `tests/unit/` and extend an e2e journey if user-facing.
